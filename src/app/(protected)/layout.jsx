export const metadata = {
  title: "Gestión ERP",
  description: "Módulos operativos y administrativos de Blue Ingeniería SPA.",
  robots: {
    index: false,
    follow: false,
  },
};

export default function ProtectedLayout({ children }) {
  return children;
}
